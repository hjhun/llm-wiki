# Ingest progress

- Updated: _아직 ingest 실행 이력 없음_
- Leaves: 0/0 done

| leaf | status | sub-chunks done/total | last_error | session |
| --- | --- | --- | --- | --- |
| _(empty)_ | — | — | — | — |

_Resume by running `/ingest` again — the next sub-chunk is picked from
`wiki/.progress/ingest/.state.json` automatically. Or run `/ingest-loop`
to let the webapp re-spawn the CLI for every remaining sub-chunk; click
"Stop loop" in the chat UI to halt after the current one._
